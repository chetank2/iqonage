<!-- Who Can Make an Icon — Counter -->
<section class="counter-section" id="counterSection">
  <div class="section-header">
    <h2>Who can Make an Icon</h2>
    <div class="line"></div>
  </div>
  <div class="counter-stage" id="counterStage">
    <div class="counter-row">
      <div class="counter-number" id="counterNumber">0</div>
      <div class="counter-label-wrap" id="counterLabelWrap">
        <div class="counter-label is-active" id="counterLabel">Strategists</div>
      </div>
    </div>
    <div class="counter-finale-logo" id="finaleLogo">IQON AGE</div>
  </div>
</section>

<style>
  .counter-section {
    padding: 0 80px 60px;
  }
  .counter-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 55vh;
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  .counter-row {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 24px;
    flex-wrap: wrap;
  }
  .counter-number {
    font-family: var(--font-display);
    font-weight: 100;
    font-size: clamp(72px, 12vw, 180px);
    color: var(--light1);
    white-space: nowrap;
    line-height: 1;
    opacity: 0;
    transition: font-size 0.6s cubic-bezier(0.23, 1, 0.32, 1);
  }
  .counter-label-wrap {
    position: relative;
    height: clamp(40px, 5.5vw, 80px);
    overflow: hidden;
  }
  .counter-label {
    font-family: var(--font-display);
    font-weight: 100;
    font-size: clamp(32px, 5vw, 72px);
    color: var(--light1);
    text-transform: uppercase;
    line-height: 1;
    white-space: nowrap;
    opacity: 0;
    position: absolute;
    left: 0; top: 0;
    width: 100%;
  }
  .counter-label.is-active {
    position: relative;
  }

  .counter-finale-logo {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(48px, 8vw, 120px);
    background: linear-gradient(to bottom, var(--accent-from), var(--accent-to));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: -6px;
    margin-top: 20px;
    opacity: 0;
    user-select: none;
    transform: translateY(20px) scale(0.9);
  }
</style>
