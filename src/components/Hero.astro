<!-- Hero Section -->
<section class="hero" id="hero">
  <div class="glow glow-hero" id="glowHero"></div>
  <div class="hero-iqon-bg" id="heroIqonBg">IQON AGE</div>
  <h1 class="hero-title" id="heroTitle">Icons are created, not found</h1>
  <div class="hero-cta-wrap" id="heroCta">
    <a href="#contact" class="btn btn--md" data-magnetic><span>Make an Icon</span></a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 140px 80px 80px;
    text-align: center;
    gap: 0;
  }
  .hero-iqon-bg {
    position: relative;
    z-index: 1;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(60px, 9.5vw, 140px);
    background: linear-gradient(to bottom, var(--accent-from), var(--accent-to));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    opacity: 0;
    letter-spacing: -8px;
    white-space: nowrap;
    user-select: none;
    pointer-events: none;
    margin-bottom: -10px;
  }
  .hero-title {
    position: relative; z-index: 2;
    font-family: var(--font-display);
    font-weight: 100;
    font-size: clamp(36px, 6.5vw, 100px);
    color: var(--light1);
    opacity: 0.85;
    letter-spacing: -4px;
    line-height: 1.05;
    max-width: 1100px;
  }
  .hero-title :global(.word) {
    display: inline-block;
    opacity: 0;
    transform: translateY(50px);
  }
  .hero-cta-wrap {
    position: relative; z-index: 2;
    margin-top: 28px;
    opacity: 0;
    transform: translateY(30px);
  }
</style>
